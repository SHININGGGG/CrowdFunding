import { ProjectstatusPipe } from './projectstatus.pipe';

describe('ProjectstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
